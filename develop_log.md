# 开发日志
## 2025-11-19

### 添加用户聊天偏向配置页面
- 提示词  
请帮我生成一个用户聊天偏向配置页面的HTML模板,包括表单字段:用户姓名,聊天偏向(例如:专业,通用,情感支持等),提交按钮等,页面风格清新可爱,包含CSS样式
在主页左上角加入一个链接,点击后跳转到该页面
用户聊天偏向配置页面在chat目录下
用户聊天偏向配置model为chat.models.UserChatProfile

- 提示词
聊天风格偏好chat_preference.html
修改下展示样式,增加风格描述以较小字体展示在风格名称下面
风格改为
邻家暖男,霸道总裁,开朗潮男
青梅女孩,职场丽人,知心姐姐
三男三女 通过css样式区分性别
chat_preference.html也需要嵌套仅login的base.html或index.html中,可以随时切换到其它页面。


请给我讲解一下
django项目的api请求流程是什么样的？从浏览器或者postman发起请求开始讲。
要求以chat.view.py 的chat函数为例
并且结合django的用户系统,在调用chat函数时传入用户id,以便于获取用户信息。

## 2025-11-20
交互设计思路
    以对话+操作共同构成交互方式
    例如：
        用户登录后进入主页进行对话式交互,根据用户输入内容,可以有如下操作
        1. 直接回复
        2. 调用工具并直接回复
        3. 跳转到计划/任务/文档库等页面进行后续操作,这些页面都已操作表单为主,根据表单内容对话为辅。
    全局会话共享聊天记录
    功能组成
        对话
        主题
            主题是对话的核心,用户对话时会自动判断意图切换主题
            主题的构成有
                文档集
                计划
                事项
                主题总结(每天更新)
    会话组成
        系统提示词
            功能提示词
            用户聊天偏向配置
            当前功能数据
        用户提示词
            全局聊天记录
            当前用户输入

## 丢失了到2026-01-28的开发日志
大概为实现了可计划的agent
实现了rag功能 文档上传/拆解/向量化/查询
rag接入agent,通过节点实现了 知识库搜搜/web搜索/混合搜索

## 2026-01-28
清理了长期开发未push导致的各种问题
5轮对话注入agent

准备进行事项开发功能,
事项包括 一次性事项/重复事项/个人OKR
一次性事项/重复事项为基础功能,用户可以独立创建,有取消/超时/进行中/完成4个状态
重复事项可以设置重复周期,例如:每天/每月/每年(每周几/每月第几天等等) 执行精度到小时即可
个人OKR为长期事项,用户根据一个大的目标创建,根据OKR规则拆解为细分项目,细分项目为一次性事项和重复事项
按照目标O产生多个可量化指标KR
KR产生一个或多个一次性事项/重复事项这个逻辑设计功能

你需要做到
1. 完全理解需求,产生出产品/技术方案
2. 与我(用户)进行沟通确认,确认后进行下一步,否则停止
3. 设计数据库表结构,并等待用户确认
4. 完成model的创建
5. 完成service层的创建
6. 完成route层的创建
7. 完成前端view层的创建
    - chat聊天页面 右侧添加当月日历,日期存在进行中事项时则使用淡绿色在日期底部标记,超时灰色标记/完成使用淡蓝色标记 取消无标记
    - 入口 toolbar[事项管理]
        - 事项列表 包含一次性事项/重复事项/个人OKR 点击可以跳转到事项详情页 可以在列表页进行删除/取消操作
        - 事项详情
            一次性事项 展示事项基本信息及完成情况 可以进行取消/调整时间/内容等操作
            重复事项 展示事项基本信息及重复周期 可以进行取消/调整时间/内容等操作
            个人OKR 展示OKR基本信息,使用树状或表格展示O与KR与事项的关系,下方有列表展示所有待执行事项,点击可以跳转到事项详情页（OKR创建的事项不展示在事项列表中）
        - 新增事项 支持手动创建于AI创建两种 先实现手动创建 AI创建待确认功能无误后再根据定版代码生成。

## 2026-01-29
- 事项页面开发/调整

# 2026-01-30
表结构调整:
    tasks事项表: 事项基础信息记录（名称/类型[一次性/重复/个人OKR]/描述/起止时间/状态等）。
    task_instances: 事项实例表[执行项目/KR],事项的执行情况记录单位,包含标题,执行明细,起止时间和状态。

    表联系 
    - task
        - task_instances[执行项目]
        - task_instances[执行项目]
        - task_instances[KR]
            - task_instances[执行项目]
            - task_instances[执行项目]

# 2026-02-04:
trae agent删除了我之前手工修改好的代码。我现在只能把相关代码都清理感觉然后重新开始。
经过这段时间的对事项功能的不断调整,我决定将事项更名为目标。并且只保留一个创建目标入口。

并且我发现trae新增了skills功能,我将尝试使用skills对单个模块开发进行管理,我将把需求文档,技术方案都写在skills中。
## 目标功能需求如下：
用户可以通过创建目标来规划自己的生活,包括工作/学习/生活等。每个目标都对应一个或者多个执行情况,可以根据跟踪执行情况来更新目标状态和记录目标的执行过程。
用户可以选择AI创建和规划目标来让AI帮助用户更好的执行目标。
用户可以随时取消目标/执行情况,系统会自动更新状态为已取消。
每个执行情况都有标题/执行内容/执行结果。执行结果默认为执行成功。用户也可以选择手动填写。每天根据执行情况的执行结果更新目标上下文,用来规划下一个执行情况。
用户可以在主界面与agent对话时对目标进行提问,当用户涉及到修改和调整目标时,agent引导用户跳转到对应的目标详情页。
目标详情页面有agent对话框,与其它agent共享对话记录。主要针对于当前目标进行问答,通过目标上下文来提供目标基础信息。
目标可以选择绑定知识库,用户在目标详情页提问时,会自动关联知识库中文档。
### 创建目标
milk_note为了更好的辅助用户记录自己的目标,需要实现目标管理功能。用户可以创建/查看/删除/修改目标。
不光需要让用户制定目标,还需要结合AI能力监督和辅助用户执行目标,当用户创建目标后,对话主页中将展示目标列表。
并且在AI对话时,会提示用户今日代办目标情况,与执行建议。(每天1次)
每个目标都有完成度,根据目标执行情况自动更新。
目标由AI规划选项,如果开启则会由AI进行待办事项的定期更新。
目标状态为:未开始/进行中/已完成/已取消
目标分为三种类型(一次性/重复/复杂目标)
- 一次性目标的作用是简单的备忘录,用户可以输入待办事项并设置执行时间,系统会自动记录并展示在目标列表中。
- 重复目标的作用是重复执行的事项,用户可以设置重复周期和起止时间,例如:每天/每周/每月等。
- 复杂目标使用OKR规则,用户可以创建一个复杂的目标,通过将单个复杂目标拆分为多个子目标。
    - 子目标: 子母标可以是一次性目标或重复目标,执行逻辑与一次性目标和重复目标一致。当分页查询目标时,子目标不展示。
执行时间单位到天即可。
举例:
1. 用户在2026-02-07需要去买年货,购物清单为:xxxx。此时用户可以创建一个一次性目标,目标标题为:购买年货,目标描述为:购买年货,购物清单为xxxx,目标执行时间为2026-02-07。
2. 用户需要在2026年维持健身习惯,可以在2026-01-01到2026-12-31创建一个重复目标,目标标题为:健身房,目标描述为: 2027年目标减重到75kg,体脂率到20%。重复周期为每周一、三、五。
3. 用户需要在半年内完成学习python的目标,可以在2026-01-01到2026-06-30创建一个复杂目标-学习python,目标描述为:学习python,了解python基本语法,完成一个python web项目开发。 模拟面试5次。
可以根据此复杂目标创建子目标:
    1. 了解python基础语法[重复目标] 2026-01-01到2026-01-07  
    2. 基础语法考试[一次性目标] 2026-01-08
    3. 了解python语言web框架[重复目标] 2026-01-09到2026-01-15
    4. 完成python web项目[一次性目标] 2026-06-30
    5. 模拟面试[一次性目标] 2026-04-01
    6. 模拟面试[一次性目标] 2026-05-01
    7. 模拟面试[一次性目标] 2026-06-01 

**页面需要注意**:需要引导用户先选择目标类型,不同目标类型创建时需要填写的信息不一样。

### 创建待办事项
入口: 目标管理页按钮
待办事项有两种创建方式：
- 用户手动创建: 用户可以在目标详情页通过添加待办事项来创建待办事项。
- AI创建: 当目标创建完成,并开启AI规划选项,根据目标类型,由AI定期创建待办事项。
    - 一次性目标: 目标创建后,由程序同步创建,执行时间,标题,内容与目标保持一致,无需AI介入。
    - 重复目标: 目标创建后,由AI根据标题/内容/重复周期/起止时间,自动创建待办事项。每次最多创建一周内的待办事项。每周定期根据目标执行上下文更新下一周的待办事项。
    - 复杂目标: 目标创建后,由AI根据OKR规则,自动创建子目标,子目标开启AI规划选项。子目标规划待办事项时,需要参考父目标和子目标的目标内容,并根据目标执行上下文来规划待办事项。

待办事项是目标拆解后的具体实施步骤。用户可编辑执行内容，执行结果。
待办事项状态有: 未开始/进行中/已完成/已取消/已超时,默认为未开始状态,每天0点根据执行时间自动更新状态。
执行结果默认为执行成功,用户也可以手动填写,支持最高2000字。
举例:
1. 购买年货目标 自动创建一个执行情况,标题为:购买年货,购物清单为xxxx 状态为未开始。
2. 健身房目标 自动创建第一个星期的三条执行情况。
    - 标题为: 健身房1 内容为:椭圆机50分钟 配速xx 执行日期: xxxx-xx-xx。 
    - 标题为: 健身房2 内容为:爬坡50分钟 配速xx 执行日期: xxxx-xx-xx。
    - 标题为: 健身房3 内容为:慢跑x50分钟 配速xx 执行日期: xxxx-xx-xx。
3. 学习python目标,根据子目标类型创建执行情况,逻辑与一次性目标和重复目标相同。

### 目标详情
入口: 点击目标卡片
目标详情是一个独立页面,可以展示目标的基本信息。用户可以在当前页面与agent对话,根据当前目标进行提问。
- 一次性目标： 展示基础信息与执行情况即可,用户可以在当前页面添加待办事项。
- 重复目标: 除基础信息外,展示重复周期与起止时间。并展示当前周待办事项的执行情况。可以通过待办事项管理按钮召出弹窗对所有待办事项进行CRUD操作。
- 复杂目标: 除基础信息外,展示子目标基础信息与类型,点击子母表则根据子目标类型进入一次性/重复目标详情页。

### AI规划
每天0点扫描所有父目标,根据目标类型/标题/内容/上下文与当天待办事项执行情况更新目标上下文,并判断是否需要调整计划。若需要调整计划,则记录调整建议。当下次用户访问时,以对话形式提示用户调整建议,当用户点击确认后,根据调整建议执行待办事项调整方法。
同


### 任务管理
入口位置: toolbar[目标管理]
点击跳转到目标列表页,可以新增/搜索/删除/编辑目标。
列表以卡片形式展示,每个卡片通过颜色与图片区分类型与状态。卡片展示目标标题/内容与执行进度。点击卡片进入目标详情页。

### 其它功能
常规增删改查操作,请注意目标和待办事项都没有删除操作,只有取消。

## 前端开发要求
请你仔细阅读需求,完全理解后,先阅读api/route/endpoints/task.py文件,此文件为任务管理的api接口。所有接口开发请参考此文件,前端开发阶段忽视其它后端文件。
前端api接口存储目录位于web/src/api目录下。
前端组成部分有:
- 目标管理[单独页面],包含功能: 创建目标/查看目标/删除目标/修改目标/搜索目标
- 目标详情[单独页面],包含功能: 根据类型展示不同目标的全部信息,并展示执行情况与待办事项。可以在目标详情页面与agent对话,根据当前目标进行提问。同时展示周一到周日的日历,显示[周一、周二、周三、周四、周五、周六、周日],根据当天是否有待办事项与执行情况,分别使用淡绿色在日期底部标记,超时灰色标记/完成使用淡蓝色标记 取消/无待办无标记。
- 待办事项管理[弹窗],包含功能: 创建待办事项/查看待办事项/删除待办事项/修改待办事项/搜索待办事项
    - 其中修改和查看待办事项使用单个弹窗分屏展示。而非弹窗内部再弹窗。

请先阅读需求,并分析需要的前端组件,所有前端组件和样式优先选用web/src/styles和web/src/components目录下的组件。
组件中包含所有必要的样式和功能,无需额外创建,若不存在则根据需求创建。
创建新组件时请分析是否可以提取为通用组件/样式,如果是则在components目录下创建新组件/样式文件,否则写在对应的vue文件中即可。

前端样式以橙/白/蓝为主要颜色,其他颜色根据需要选择,符合产品轻松简约愉快的治愈系风格。
单独页面需要在base.html或index.html中嵌套,可以随时切换到其它页面。

## 后端开发要求
后端文件目录: 
- api/route/endpoints/task.py: 任务管理接口(API)。
- api/models/task.py: 任务模型(ORM)。
- api/services/task.py: 任务服务层(业务逻辑)。
- api/schemas/task.py: 任务数据模型(API请求/响应)。
- api/task: 定时任务
- api/core: 核心目录(配置文件/数据库连接/通用工具类/常量等)

请阅读需求,并分析后端需要的模型和方法,在对应目录/文件中找到实现。如果有则判断是否满足要求,不满足则提示用户需要修改/创建并展示修改建议。
用户同意后进行修改。
请保证功能具有良好的可维护性,结构清晰注释完善。
请严格按照model/service/api分层开发。

# 2026-02-05
完成表结构设计/model/service/taks开发
开始开发task agent,用来整理计划context

# 2026-02-09
ai总结目标上下文 done
ai生成每周的重复人物执行单元 done

# 2026-02-10 
ai生成复杂目标的子目标 done
目标模块route层开发 done
route层调试 done

# 2026-02-11
目标对话功能开发,设计agent
## 节点设计
- **think** 思考节点:
作用为根据目标上下文(包含子目标),判断答复用户需要执行的前置操作。
根据 用户提问 目标上下文 当前时间 生成 agent需要执行的操作
next: execute

- **execute** 执行节点:
判断操作执行状态,如果需要执行,则跳转到对应节点。若执行完成/失败,则跳转到summary/execute_finish节点。
next: summary/fail_feedback/web_search/doc_search

- **web_search** 搜索节点(创建时指定retry_count = 3): 
联网搜索节点,通过互联网查询资料,作为参考回答用户问题。
next: execute

- **doc_search** 文档搜索节点:
通过目标绑定的知识库,调取文档作为参考回答用户问题。
next: execute

- **child_search** 子目标搜索节点:
根据子目标类型,调用子目标详情页,展示子目标基本信息与执行情况。
next: execute

- **summary** 总结节点:
结合目标上下文与前置操作结果,生成对用户输入的回复。(此处可以注入用户对于agnet的回复风格喜好)
next: END

- **fail_feedback** 失败反馈节点:
简单返回“回复中出现了一些意料之外的问题,请稍后重试~”。
next: END
# 2026-03-03
实现了针对目标的问答功能,目前接入联网搜索,尚未接入知识库
使用单个搜索节点根据搜索计划节点生成的搜索任务列表进行搜索操作
搜索完成后使用回答结果进行答复。

现在应该完成知识库的接入。

## 思考
需要简单记录下目前milk note的整体情况了
通过ai辅助开发完成了三个agent
- 主agent: 主界面用户交互,上下文中注入用户目标管理包含最近待办事项和近期执行情况。
- 目标管理agent：定时任务调度,定时总结用户目标执行情况,并生成调整建议。
- 目标对话agent: 聚焦于单个目标的对话agent,可以通过对话形式与用户交互,通过联网搜索方式回答用户与当前目标相关的问题。
主agent与目标对话agent作为会话型agent应共享用户对话历史记录。

关于目标与知识库
每个目标是一个独立的知识库,可以通过上传的方添加用户文档。用户文档切片与向量化之后存储到pgsql数据库中。
当用户对目标进行提问时,判断是否存在绑定的知识库,如果存在则根据用户历史对话提取关键信息查询相关文档,并将排名靠前的5个文档切片作为上下文注入到目标对话agent中。

# 2026-03-04
## 思考
按照以往开发逻辑,web应用由controller/service/model组成。
在传统web应用中加入agent,向上对用户提供对话式交互功能,向下可以做CRUD操作。
那么agent层的位置应该在controller和service之间。由controller做前置校验,校验通过后,调用当前controller层业务agent处理用户请求。
controller层依然支持传统直接调用service层方法的方式。
agent间通过共享service层的方式实现上下文共享。
service层的特定方法可以封装为tool,供agent调用。


# 2026-03-06
## 计划
这两天没时间干,先记录一下
目前已经实现了文档与目标绑定的功能,接下来要做的是在目标对话时判断是否有绑定文档,如果有的话需要提取关键词并查询文档

调整了一下目录层级 现在agnet和service同级 避免循环依赖 也更符合业务需求